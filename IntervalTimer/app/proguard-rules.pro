# Add project specific ProGuard rules here.
# No reflection-heavy libraries are used beyond DataStore/Compose/Play Services Wearable,
# which ship their own consumer-rules.pro, so no custom -keep rules are required today.
